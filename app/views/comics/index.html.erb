<%= link_to "deconnexion", destroy_user_session_path, method: :delete%>

<%= render "shared/banner" %>
<div class="wrapper display-home">
<div class="container">
  <div class="row">
  <% @comics.each do |c| %>
    <div class="col-xs-12 col-sm-4 col-lg-3">

        <%= render "comic", comic: c %>

    </div>
     <% end %>
  </div>
</div>
</div>
